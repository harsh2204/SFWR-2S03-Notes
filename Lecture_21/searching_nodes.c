//Searching Linked Lists
//
//struct node *search(struct node* list, int n)
//{
//	while(list!= NULL && list -> value != n)
//		list = list -> next;
//	return list;
//}
//
//struct node *search(struct node* list, int n)
//{
//	if (list == NULL or list->value==n))
//		return list;
//	return search(list->next, n);
//}
//
